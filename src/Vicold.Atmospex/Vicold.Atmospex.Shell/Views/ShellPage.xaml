<Page
    x:Class="Vicold.Atmospex.Shell.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:local="using:Vicold.Atmospex.Shell.Helpers.Converters"
    xmlns:layer="using:Vicold.Atmospex.Layer"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">
    <Page.Resources>
        <local:FloatFormatConverter x:Key="FloatFormatConverter"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="40" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.ColumnSpan="2" x:Name="AppTitleBar">
            <Image Source="/Assets/WindowIcon.ico" HorizontalAlignment="Left" Width="16" Height="16" Margin="8,0"/>
            <TextBlock x:Name="AppTitleBarText" VerticalAlignment="Center" TextWrapping="NoWrap" Style="{StaticResource CaptionTextBlockStyle}" Margin="32, 0, 0, 0"/>
        </Grid>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <MenuBar Grid.Column="0">
                <MenuBarItem x:Uid="ShellMenuBarItem_File" >
                    <MenuFlyoutItem x:Uid="ShellMenuItem_File_Exit" Command="{x:Bind ViewModel.MenuFileExitCommand}" />
                </MenuBarItem>
                <MenuBarItem x:Uid="ShellMenuBarItem_Views">
                    <MenuBarItem.Items>
                        <MenuFlyoutItem x:Uid="ShellMenuItem_Views_Main" Command="{x:Bind ViewModel.MenuViewsMainCommand}" />
                    </MenuBarItem.Items>
                </MenuBarItem>
                <MenuBarItem x:Name="ProjectionMenuBarItem" Title="投影">
                    <MenuFlyoutItem Text="墨卡托投影" Command="{x:Bind ViewModel.SetProjectionCommand}" CommandParameter="Mercator" />
                    <MenuFlyoutItem Text="等经纬度投影" Command="{x:Bind ViewModel.SetProjectionCommand}" CommandParameter="EqualLonLat" />
                    <MenuFlyoutItem Text="近似真实投影" Command="{x:Bind ViewModel.SetProjectionCommand}" CommandParameter="CloseToReal" />
                    <MenuFlyoutItem Text="兰伯特投影" Command="{x:Bind ViewModel.SetProjectionCommand}" CommandParameter="Lambert" />
                </MenuBarItem>
            </MenuBar>
            <AppBarButton Grid.Column="1" x:Name="ShellMenuBarSettingsButton" x:Uid="ShellMenuBarSettingsButton"
                          AnimatedIcon.State="Normal"
                          PointerEntered="ShellMenuBarSettingsButton_PointerEntered"
                          PointerExited="ShellMenuBarSettingsButton_PointerExited"
                          Command="{x:Bind ViewModel.MenuSettingsCommand}">
                <AnimatedIcon x:Name="ShellMenuBarSettingsButtonIcon">
                    <AnimatedIcon.Source>
                        <animatedvisuals:AnimatedSettingsVisualSource/>
                    </AnimatedIcon.Source>
                    <AnimatedIcon.FallbackIconSource>
                        <FontIconSource FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE713;"/>
                    </AnimatedIcon.FallbackIconSource>
                </AnimatedIcon>
            </AppBarButton>
        </Grid>
        <Grid Grid.Row="2" VerticalAlignment="Center">
            <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" HorizontalAlignment="Left">
                <AppBarButton Icon="Home" x:Uid="ShellCommandBar_Home" Command="{x:Bind ViewModel.MenuViewsMainCommand}">
                    <AppBarButton.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="D" />
                    </AppBarButton.KeyboardAccelerators>
                </AppBarButton>
                <AppBarButton Icon="Add" x:Uid="ShellCommandBar_Open" Command="{x:Bind ViewModel.MenuFileOpenCommand}"/>
                <AppBarButton Icon="Edit" x:Uid="ShellCommandBar_Edit"/>
                <AppBarButton Icon="RepeatAll" x:Uid="ShellCommandBar_ResetView"  Command="{x:Bind ViewModel.MenuResetViewCommand}"/>
                <AppBarButton Icon="Cancel" x:Uid="ShellCommandBar_Cancel"/>
                <CommandBar.SecondaryCommands>
                    <AppBarButton Icon="Setting" x:Uid="ShellCommandBar_Settings">
                        <AppBarButton.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="I" />
                        </AppBarButton.KeyboardAccelerators>
                    </AppBarButton>
                </CommandBar.SecondaryCommands>
            </CommandBar>
        </Grid>

        <Grid Grid.Row="3" >
            <Frame x:Name="NavigationFrame" Padding="0"/>
        </Grid>

        <Grid Grid.Row="2" Grid.RowSpan="2" Grid.Column="1" BorderThickness="1" BorderBrush="Gray">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" HorizontalAlignment="Left">
                <AppBarButton Icon="View"  Label="Hidden"/>
                <AppBarButton Icon="Cancel" Label="Clear"/>
            </CommandBar>
            
            <ScrollViewer Grid.Row="1" Margin="5" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ListView x:Name="LayerListView" ItemsSource="{x:Bind ViewModel.Layers}"
                          SelectionMode="Single" Height="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="layer:ILayer">
                            <Grid Padding="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" Margin="5,0"/>
                                <ToggleSwitch Grid.Column="1" IsOn="{x:Bind IsVisible, Mode=TwoWay}" Margin="5,0" Toggled="LayerVisibility_Toggled" OnContent="" OffContent="" Width="15" HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
            
        </Grid>

        <Grid Grid.Row="4" Grid.ColumnSpan="2" x:Name="FootBar" Padding="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Uid="ShellPage_LongitudeText" Margin="10,0,0,0"/>
                <TextBlock x:Name="LongitudeTextBlock" Text="{x:Bind ViewModel.LongtitudeDisplay, Mode=OneWay}" Width="100"/>
                <TextBlock x:Uid="ShellPage_LatitudeText" />
                <TextBlock x:Name="LatitudeTextBlock" Text="{x:Bind ViewModel.LatitudeDisplay, Mode=OneWay}" Width="100"/>
                <TextBlock Text="FPS: " />
                <TextBlock x:Name="FpsTextBlock" Text="{x:Bind ViewModel.Fps, Mode=OneWay}" Width="80" HorizontalAlignment="Left" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
