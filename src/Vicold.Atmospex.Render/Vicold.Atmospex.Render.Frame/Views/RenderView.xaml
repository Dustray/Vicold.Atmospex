<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Vicold.Atmospex.Render.Frame.Views.RenderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vicold.Atmospex.Render.Frame.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" HorizontalAlignment="Left">
            <AppBarButton Icon="Add" Label="Open" Click="OpenLogButton_Click"/>
            <AppBarButton Icon="Edit" Label="Edit"/>
            <AppBarButton Icon="RepeatAll" Label="ResetView" Click="ResetCamera_Click"/>
            <AppBarButton Icon="Cancel" Label="Cancel" Click="ClearButton_Click"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Setting" Label="Settings">
                    <AppBarButton.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="I" />
                    </AppBarButton.KeyboardAccelerators>
                </AppBarButton>
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <SwapChainPanel x:Name="SwapChainPanel" 
                   Grid.Row="1"
                   IsTabStop="True" 
                   IsHitTestVisible="True"
                   PointerPressed="OnSwapChainPanelPointerPressed"
                   PointerReleased="OnSwapChainPanelPointerReleased" />
    </Grid>
</UserControl>
